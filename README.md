
# 📘 Контрольна робота з ООП (C++)

## 📅 Дата: 10.12.2025 19:00-20:00

## 🧾 Формат здачі

1. Створити бранч
   **`10_12_25/<name>`**

2. Додати у нього файл:
   **`10_12_25/<name>/main.cpp`**

3. У `main.cpp` реалізувати *свій варіант завдання*.

4. Створити Pull Request у `main`.

---

# ❗ Важлива вимога

### **У функції `main()` кожен студент має продемонструвати виконання ВСІХ пунктів свого завдання:**

* створення об’єктів різними конструкторами
* використання деструктора (неявно)
* виклик спеціальних методів класу
* роботу перевантажених операторів
* роботу з масивом об’єктів
* пошук максимального / мінімального елемента за критерієм
* обчислення суми / середнього / іншої колективної характеристики
* вивід результатів на екран
* завантаження даних з файлу
* запис результатів у файл

---

# 👥 Розподіл варіантів між студентами

| №  | Студент                  | Варіант          |
| -- | ------------------------ | ---------------- |
| 1  | zhuravliov_artem         | Vector2D         |
| 2  | dybskyi_bohdan           | Rational         |
| 3  | nikitchuk_artur          | Time             |
| 4  | kroshnyi_severyn         | Student          |
| 5  | rymyk_nadiia             | Triangle         |
| 6  | matsiievska_sofiia       | Rectangle        |
| 7  | kotsemira_damian         | Date             |
| 8  | mostyka_artem            | Book             |
| 9  | maryniak_andrii          | Point3D          |
| 10 | fedyk_vadym              | BankAccount      |
| 11 | yatsuliak_sofiia         | FractionalNumber |
| 12 | klymovych_roman          | ComplexPolar     |
| 13 | storozhenko_oksana       | Circle           |
| 14 | povoroznyk_mykola        | Movie            |
| 15 | haiduk_yurii             | Temperature      |
| 16 | hotsii_danylo            | Currency         |
| 17 | kovalchuk_ihor           | Polynomial2      |
| 18 | strontsitskyi_sviatoslav | Car              |
| 19 | tymkiv_nadiia            | Worker           |
| 20 | fedoruk_demian-marian    | Matrix2x2        |
| 21 | khomiak_oleksandr        | Flight           |
| 22 | mazepa_andrii            | LineSegment      |
| 23 | cherniak_daria           | Laptop           |

---

# 🧮 Детальні описи варіантів

---

## **1. Vector2D (x, y)** 🔹 *zhuravliov_artem*

Клас для роботи з двовимірними векторами.

**Атрибути:**
- `x`, `y` (double) — координати вектора

**Методи:**
- Конструктори: без параметрів (0, 0), з параметрами (x, y)
- `length()` — обчислює довжину вектора: $\sqrt{x^2 + y^2}$
- `add(Vector2D)` або оператор `+` — додавання векторів
- Оператор `>`, `<` — порівняння за довжиною
- Деструктор (неявно)

**Завдання в main():**
- Створити 5 векторів різними конструкторами
- Знайти найдовший вектор
- Обчислити суму всіх векторів
- Вивести всі результати

---

## **2. Rational (p/q)** 🔹 *dybskyi_bohdan*

Клас для роботи з раціональними числами (дробами).

**Атрибути:**
- `numerator` (int) — чисельник
- `denominator` (int) — знаменник

**Методи:**
- Конструктори: без параметрів (0/1), з параметрами (p, q)
- `reduce()` — скорочення дробу за НОД
- Оператор `+` — додавання дробів
- Оператор `*` — множення дробів
- Оператор `>`, `<` — порівняння за величиною
- `toDouble()` — перетворення у дійсне число

**Завдання в main():**
- Створити масив 5 дробів
- Знайти найбільший дріб
- Обчислити суму всіх дробів
- Звести дроби до простої форми

---

## **3. Time (год:хв:сек)** 🔹 *nikitchuk_artur*

Клас для роботи з часом.

**Атрибути:**
- `hours`, `minutes`, `seconds` (int)

**Методи:**
- Конструктори: за замовчуванням (0:0:0), з параметрами
- `normalize()` — коректне представлення часу (60 сек → +1 хв)
- Оператор `+` — додавання часу
- Оператор `>`, `<` — порівняння часових моментів
- `getTotalSeconds()` — всього секунд від початку дня

**Завдання в main():**
- Створити масив 5 часових моментів
- Знайти найпізніший час
- Обчислити сумарну тривалість всіх часів
- Вивести часи у форматі ГГ:ММ:СС

---

## **4. Student (ПІБ, бал)** 🔹 *kroshnyi_severyn*

Клас для роботи з інформацією про студентів.

**Атрибути:**
- `name` (string) — повне ім'я студента
- `score` (double) — бал (0–100)

**Методи:**
- Конструктори: без параметрів, з параметрами
- `isValid()` — перевірка коректності балу
- Оператор `>` — порівняння за балом
- `setScore(double)` — встановлення балу з валідацією
- Оператор `<<` — вивід на екран

**Завдання в main():**
- Створити масив 5 студентів
- Валідувати дані
- Знайти найкращого студента (максимальний бал)
- Обчислити середній бал групи
- Записати результати у файл

---

## **5. Triangle (a, b, c)** 🔹 *rymyk_nadiia*

Клас для роботи з трикутниками.

**Атрибути:**
- `a`, `b`, `c` (double) — довжини сторін

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- `isValid()` — перевірка нерівності трикутника
- `perimeter()` — периметр
- `area()` — площа за формулою Герона: $S = \sqrt{p(p-a)(p-b)(p-c)}$, де $p = \frac{a+b+c}{2}$
- Оператор `>`, `<` — порівняння за площею

**Завдання в main():**
- Створити масив 5 трикутників
- Валідувати кожен трикутник
- Знайти найбільший (за площею)
- Обчислити загальний периметр усіх трикутників
- Записати результати

---

## **6. Rectangle (w, h)** 🔹 *matsiievska_sofiia*

Клас для роботи з прямокутниками.

**Атрибути:**
- `width`, `height` (double)

**Методи:**
- Конструктори: за замовчуванням (1x1), з параметрами
- `area()` — площа = w × h
- `perimeter()` — периметр = 2(w + h)
- Оператор `*` — масштабування (Rectangle * коефіцієнт)
- Оператор `>`, `<` — порівняння за площею

**Завдання в main():**
- Створити масив 5 прямокутників
- Масштабувати деякі з них
- Знайти найбільший прямокутник
- Обчислити суму всіх площ
- Вивести результати

---

## **7. Date (день, місяць, рік)** 🔹 *kotsemira_damian*

Клас для роботи з датами.

**Атрибути:**
- `day`, `month`, `year` (int)

**Методи:**
- Конструктори: 01.01.1900, з параметрами
- `isValid()` — перевірка коректності дати (враховуючи високосні роки)
- Оператор `<`, `>` — порівняння дат
- `addDays(int)` — додавання днів до дати
- `daysUntil(Date)` — кількість днів між датами

**Завдання в main():**
- Створити масив 5 дат
- Валідувати дати
- Знайти найраніш та найпізнішу дату
- Додати дні до деяких дат
- Обчислити проміжки між датами

---

## **8. Book (назва, автор, рік)** 🔹 *mostyka_artem*

Клас для роботи з інформацією про книги.

**Атрибути:**
- `title` (string) — назва
- `author` (string) — автор
- `year` (int) — рік видання

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- Оператор `>`, `<` — порівняння за роком видання
- `getAge()` — скільки років від видання
- Оператор `<<` — красивий вивід

**Завдання в main():**
- Створити масив 5 книг
- Знайти найстарішу книгу
- Обчислити середній рік видання
- Вивести книги відсортовані за роком
- Записати результати у файл

---

## **9. Point3D (x, y, z)** 🔹 *maryniak_andrii*

Клас для роботи з точками у 3D просторі.

**Атрибути:**
- `x`, `y`, `z` (double)

**Методи:**
- Конструктори: (0, 0, 0), з параметрами
- `distanceFromOrigin()` — відстань до (0,0,0): $\sqrt{x^2 + y^2 + z^2}$
- `distance(Point3D)` — відстань між двома точками
- Оператор `+` — додавання координат двох точок
- Оператор `>`, `<` — порівняння за відстанню від origem

**Завдання в main():**
- Створити масив 5 точок
- Знайти найдальшу від origen точку
- Обчислити сумарну відстань всіх точок від origem
- Знайти відстані між точками
- Вивести результати

---

## **10. BankAccount (номер, баланс)** 🔹 *fedyk_vadym*

Клас для роботи з банківськими рахунками.

**Атрибути:**
- `accountNumber` (string) — номер рахунку
- `balance` (double) — баланс

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- `deposit(double)` — поповнення рахунку
- `withdraw(double)` — зняття з рахунку (з перевіркою баланса)
- `getBalance()` — отримати баланс
- Оператор `>`, `<` — порівняння за балансом

**Завдання в main():**
- Створити масив 5 рахунків
- Виконати операції депозиту/зняття
- Знайти рахунок з максимальним балансом
- Обчислити загальну суму грошей на всіх рахунках
- Вивести звіт

---

## **11. FractionalNumber (мантиса + порядок)** 🔹 *yatsuliak_sofiia*

Клас для роботи з числами у форматі мантиса × 10^порядок.

**Атрибути:**
- `mantissa` (double) — мантиса (1 ≤ |m| < 10)
- `exponent` (int) — порядок

**Методи:**
- Конструктори: за замовчуванням (0), з параметрами
- `toDouble()` — перетворення у звичайне число: $m \times 10^e$
- `normalize()` — нормалізація мантиси
- Оператор `>`, `<` — порівняння чисел
- Оператор `+` — додавання чисел

**Завдання в main():**
- Створити масив 5 чисел у науковому форматі
- Нормалізувати їх
- Знайти найбільше число
- Обчислити суму
- Вивести у науковому форматі

---

## **12. ComplexPolar (r, φ)** 🔹 *klymovych_roman*

Клас для роботи з комплексними числами у полярній системі координат.

**Атрибути:**
- `r` (double) — модуль
- `phi` (double) — аргумент (у радіанах)

**Методи:**
- Конструктори: за замовчуванням (1, 0), з параметрами
- `toCartesian()` — перехід у декартові координати $(x, y)$: $x = r \cos(\phi)$, $y = r \sin(\phi)$
- `magnitude()` — модуль числа (r)
- Оператор `+` — додавання комплексних чисел
- Оператор `>`, `<` — порівняння за модулем

**Завдання в main():**
- Створити масив 5 комплексних чисел
- Конвертувати до декартових координат
- Знайти число з найбільшим модулем
- Виконати додавання
- Вивести результати

---

## **13. Circle (радіус)** 🔹 *storozhenko_oksana*

Клас для роботи з колами.

**Атрибути:**
- `radius` (double)

**Методи:**
- Конструктори: за замовчуванням (r=1), з параметром
- `area()` — площа: $\pi r^2$
- `circumference()` — довжина кола: $2\pi r$
- `diameter()` — діаметр: $2r$
- Оператор `>`, `<` — порівняння за площею

**Завдання в main():**
- Створити масив 5 кіл
- Знайти коло з найбільшою площею
- Обчислити суму всіх площ
- Обчислити середню довжину кола
- Записати результати

---

## **14. Movie (назва, рейтинг)** 🔹 *povoroznyk_mykola*

Клас для роботи з інформацією про фільми.

**Атрибути:**
- `title` (string) — назва
- `rating` (double) — рейтинг (0–10)

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- Оператор `>`, `<` — порівняння за рейтингом
- `isGood()` — перевірка рейтингу > 7
- Оператор `<<` — красивий вивід

**Завдання в main():**
- Створити масив 5 фільмів
- Знайти топ-1 фільм (максимальний рейтинг)
- Обчислити середній рейтинг
- Вивести гарні фільми (рейтинг > 7)
- Записати результати у файл

---

## **15. Temperature (°C)** 🔹 *haiduk_yurii*

Клас для роботи з температурами.

**Атрибути:**
- `celsius` (double)

**Методи:**
- Конструктори: за замовчуванням (0), з параметром
- `toFahrenheit()` — перетворення у °F: $F = C \times 9/5 + 32$
- `toKelvin()` — перетворення у K: $K = C + 273.15$
- Оператор `>`, `<` — порівняння температур
- Оператор `+` — додавання температур

**Завдання в main():**
- Створити масив 5 температур
- Конвертувати до Фаренгейта та Кельвіна
- Знайти мінімальну та максимальну температури
- Обчислити середню температуру
- Вивести результати

---

## **16. Currency (UAH)** 🔹 *hotsii_danylo*

Клас для роботи з грошовими сумами.

**Атрибути:**
- `amount` (double) — сума в гривнях
- `exchangeRates` (static) — коефіцієнти обміну (1 USD ≈ 41.5 UAH, 1 EUR ≈ 45 UAH)

**Методи:**
- Конструктори: за замовчуванням (0), з параметром
- `toUSD()` — конвертація у долари США
- `toEUR()` — конвертація у євро
- Оператор `+` — додавання сум
- `getAmount()` — отримати суму

**Завдання в main():**
- Створити масив 5 сум у гривнях
- Конвертувати до USD та EUR
- Обчислити загальну суму в UAH
- Вивести всі суми у всіх валютах
- Записати результати

---

## **17. Polynomial2 (ax²+bx+c)** 🔹 *kovalchuk_ihor*

Клас для роботи з многочленами 2-го степеня.

**Атрибути:**
- `a`, `b`, `c` (double) — коефіцієнти

**Методи:**
- Конструктори: за замовчуванням (0, 0, 0), з параметрами
- `evaluate(double x)` — значення полінома в точці $x$: $a x^2 + b x + c$
- Оператор `+` — додавання поліномів (поелементно)
- `findRoots()` — пошук коренів (якщо дискримінант ≥ 0)
- `toString()` — красивий вивід формули

**Завдання в main():**
- Створити масив 5 поліномів
- Обчислити значення в точці x=2
- Знайти поліном з найбільшим значенням у цій точці
- Виконати додавання поліномів
- Вивести результати

---

## **18. Car (марка, пробіг)** 🔹 *strontsitskyi_sviatoslav*

Клас для роботи з інформацією про автомобілі.

**Атрибути:**
- `brand` (string) — марка
- `mileage` (double) — пробіг (км)

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- `drive(double km)` — додавання пробігу
- Оператор `>`, `<` — порівняння за пробігом
- `getCondition()` — оцінка умови (за пробігом)

**Завдання в main():**
- Створити масив 5 автомобілів
- Додати пробіг деяким з них
- Знайти авто з найменшим пробігом
- Обчислити середній пробіг
- Вивести стан кожного авто
- Записати результати

---

## **19. Worker (ім'я, зарплата)** 🔹 *tymkiv_nadiia*

Клас для роботи з інформацією про робітників.

**Атрибути:**
- `name` (string) — ім'я
- `salary` (double) — зарплата

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- `raiseSalary(double percent)` — підвищення зарплати на % 
- Оператор `>`, `<` — порівняння за зарплатою
- `calculateTax()` — податок на доходи (податкова ставка 18%)

**Завдання в main():**
- Створити масив 5 робітників
- Підвищити зарплату деяким
- Знайти робітника з максимальною зарплатою
- Обчислити сумарну зарплату
- Обчислити сумарні податки
- Записати звіт

---

## **20. Matrix2x2** 🔹 *fedoruk_demian-marian*

Клас для роботи з матрицями 2×2.

**Атрибути:**
```
[a b]
[c d]
```
- `a`, `b`, `c`, `d` (double)

**Методи:**
- Конструктори: за замовчуванням (одиничної), з параметрами
- Оператор `+` — додавання матриць
- `determinant()` — визначник: $ad - bc$
- `transpose()` — транспонування
- Оператор `>`, `<` — порівняння за детермінантом

**Завдання в main():**
- Створити масив 5 матриць
- Обчислити визначники всіх матриць
- Знайти матрицю з найбільшим визначником
- Виконати додавання матриць
- Транспонувати деякі матриці
- Вивести результати

---

## **21. Flight (рейс, тривалість)** 🔹 *khomiak_oleksandr* 🔹 *khomiak_oleksandr*

Клас для роботи з інформацією про авіарейси.

**Атрибути:**
- `flightNumber` (string) — номер рейсу
- `duration` (double) — тривалість (у годинах)

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- Оператор `>`, `<` — порівняння за тривалістю
- `durationInMinutes()` — тривалість у хвилинах
- `isFastFlight()` — чи рейс довгий (> 4 годин)

**Завдання в main():**
- Створити масив 5 рейсів
- Знайти найдовший та найкоротший рейси
- Обчислити середню тривалість
- Вивести коротки рейси
- Записати результати

---

## **22. LineSegment (дві точки)** 🔹 *mazepa_andrii* 🔹 *mazepa_andrii*

Клас для роботи з відрізками.

**Атрибути:**
- `point1`, `point2` (Point або (x1,y1), (x2,y2))

**Методи:**
- Конструктори: за замовчуванням ((0,0)-(1,1)), з параметрами
- `length()` — довжина: $\sqrt{(x2-x1)^2 + (y2-y1)^2}$
- Оператор `>`, `<` — порівняння за довжиною
- `midpoint()` — середина відрізка
- `isParallel(LineSegment)` — паралельність (за нахилом)

**Завдання в main():**
- Створити масив 5 відрізків
- Обчислити довжини всіх відрізків
- Знайти найдовший відрізок
- Обчислити середню довжину
- Обчислити суму всіх довжин
- Вивести результати

---

## **23. Laptop (модель, RAM)** 🔹 *cherniak_daria* 🔹 *cherniak_daria*

Клас для роботи з інформацією про ноутбуки.

**Атрибути:**
- `model` (string) — модель
- `ram` (int) — оперативна пам'ять (ГБ)

**Методи:**
- Конструктори: за замовчуванням, з параметрами
- `isGaming()` — чи це гейминг ноутбук (RAM > 16 ГБ)
- Оператор `>`, `<` — порівняння за RAM
- `upgrade(int additionalRAM)` — розширення пам'яті

**Завдання в main():**
- Створити масив 5 ноутбуків
- Знайти найпотужніший (максимум RAM)
- Обчислити середню RAM
- Оновити деякі ноутбуки
- Вивести гейминг ноутбуки
- Записати результати

---

# 📁 Робота з файлами (обов'язково)

### Програма повинна:

* зчитати початкові дані з файлу
* виконати всі обчислення
* зберегти результати у текстовий файл

Формат довільний, але зрозумілий.

